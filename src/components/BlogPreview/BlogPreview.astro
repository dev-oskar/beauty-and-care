---
/**
 * Blog preview component for the homepage
 */
import Button from "@components/Button/Button.astro";

// Define posts directly within the component
const posts = [
  {
    id: "pielegnacja-skory-zimowa",
    data: {
      title: "Zimowa pielęgnacja skóry - jak zadbać o cerę w chłodne dni",
      date: "15.02.2025",
      excerpt: "Poznaj sprawdzone sposoby na pielęgnację skóry podczas zimowych miesięcy. Dowiedz się, jak chronić cerę przed mrozem i suchym powietrzem."
    }
  },
  {
    id: "karboksyterapia-zabieg",
    data: {
      title: "Karboksyterapia - zabieg, który podbił świat kosmetologii",
      date: "10.03.2025",
      excerpt: "Poznaj rewolucyjny zabieg, który rozwiązuje problemy z cellulitem, rozstępami i zmarszczkami. Karboksyterapia to bezpieczna i skuteczna metoda odmładzania skóry."
    }
  },
  {
    id: "kwasy-w-kosmetologii",
    data: {
      title: "Kwasy w kosmetologii - przewodnik po najpopularniejszych substancjach",
      date: "25.03.2025",
      excerpt: "Poznaj różnice między kwasami AHA, BHA i PHA oraz ich zastosowanie w różnych zabiegach kosmetycznych. Dowiedz się, który kwas najlepiej sprawdzi się przy twoich problemach skórnych."
    }
  }
];
---

<section id="blog" class="site-container mt-12 scroll-mt-10 py-8 md:py-12">
  <div class="mx-auto max-w-6xl text-center">
    <h2 class="h2 text-pretty uppercase">Nasz Blog</h2>
    <p class="mt-4 text-lg md:text-xl">
      Najnowsze artykuły o pielęgnacji, zabiegach i kosmetologii
    </p>
  </div>

  <div class="mx-auto mt-8 grid gap-6 md:grid-cols-3 md:gap-8">
    {
      posts.map((post) => (
        <div class="border-base-200 flex flex-col overflow-hidden rounded-lg border shadow-sm transition-all duration-300 hover:shadow-md">
          <div class="bg-primary-50 p-5">
            <h3 class="h4 line-clamp-2 mb-2 min-h-[3rem]">{post.data.title}</h3>
            <p class="text-primary-500 text-sm font-medium">
              {post.data.date}
            </p>
          </div>
          <div class="flex flex-grow flex-col justify-between p-5">
            <p class="line-clamp-3 text-sm text-gray-600">
              {post.data.excerpt}
            </p>
            <div class="mt-4">
              <Button
                variant="ghost"
                arrow="right"
                href={`/blog/${post.id}`}
                class="pl-0 text-sm"
              >
                Czytaj więcej
              </Button>
            </div>
          </div>
        </div>
      ))
    }
  </div>

  <div class="mt-10 flex justify-center">
    <Button href="/blog" variant="outline">
      Zobacz wszystkie artykuły
    </Button>
  </div>
</section>